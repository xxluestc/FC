# FC 项目目标与持续执行追踪

更新时间：2026-07-12
仓库：`xxluestc/FC`  
建立文档时基线提交：`6852d83`
当前上游数据基线：`7e71f39`

## 1. 文档作用

本文档维护项目实施过程中的稳定事实、阶段目标、技术决策、验证结果、阻塞项和下一步。它不是论文正文，也不记录不可核验的推测。每完成一次实质实现或实验，应更新：

1. 当前完成状态；
2. 实际使用的数据、模型和参数来源；
3. 运行命令与验证结果；
4. 新发现的限制或风险；
5. 下一步唯一优先任务。

## 2. 最终研究目标

构建面向多堆 PEMFC/动力电池系统的退化感知预测功率调控框架，使控制器能够：

```text
估计各堆当前健康状态
→ 在候选功率动作下预测未来需求、SOC、氢耗和健康变化
→ 进行多堆与电池之间的滚动功率分配
→ 执行第一步动作
→ 利用新观测修正健康状态与模型
→ 进入下一轮在线决策
```

长期算法名称暂定：

```text
EADWM-MPC / EAWM-Dreamer-MPC
事件感知退化世界模型预测功率调控
```

最终目标不是仅降低一个加权目标值，而是在功率平衡和安全约束下，公平比较氢耗、燃料电池退化、电池负担、SOC维持、功率波动、启停和多堆健康均衡。

## 3. 当前已经具备的基础

### 3.1 数据与预测链

- 刘占伟 21UBE0022 半月整车数据已整理成 1 s canonical 表，共 211,630 行、46 个连续运行片段。
- 当前预测目标为整车需求功率 `P_dem`，而不是由控制策略决定的燃料电池输出功率。
- 已有 State-Aware Direct Power Prediction：使用历史车速、加速度、需求功率、SOC、停车/路线等因果状态，分别预测 H=1/3/5/10 s。
- 当前冻结模型为 XGBoost；H=5 测试逐点 RMSE 约 22.14 kW，H=10 约 33.55 kW。
- 车辆动力学模型保留为物理可解释基线，不和直接功率预测混称。

### 3.2 单堆氢电分配链

- 当前动作是单个燃料电池的离散电流/功率档位，电池通过 `P_bat=P_dem-P_fc` 补足功率。
- 已有 Rint/SOC 电池模型、法拉第理论氢耗模型和 Beam-search MPC。
- 已对比 Instant、Constant、Perfect-preview 和 Predicted MPC。
- `scripts/run_baseline.py --mode check` 可在不依赖原始大文件时完成轻量验证。

### 3.3 退化代理链

- 刘占伟 UKF-PF 提供 6104 个稳定事件上的健康参数轨迹 `theta(k)=[i0, ih, R_ohm]`，现已包含逐事件ID、canonical行号和原始索引。
- 上游累计事件表、theta物理参数、IV模型和当前点损失表已经完成对齐审计。
- 已实现动作耦合Gamma健康状态和动态性能损失代理。当前代理的准确含义是：

```text
C_deg(I | theta_current)
= 当前健康状态下，候选工作电流对应的等效性能损失
```

- 它仍不是材料退化的直接观测；动作导致的未来健康变化由单独的Gamma状态转移承担，再通过标定映射得到 `theta(k+1)`。
- 当前 `multistack_allocator.py` 仍是占位接口，仓库尚无真正的多堆闭环。

### 3.4 当前可复现资源

仓库已增加 `data/key/` 压缩数据，可通过以下命令恢复关键输入：

```bash
python scripts/00_materialize_key_data.py
python scripts/00_materialize_key_data.py --include-li-junhao
```

李俊豪三台发动机双堆数据可用于双堆工作关系、功率贡献和一致性审计，但不能直接作为整车需求预测数据，也不能自动证明长期退化因果关系。

## 4. 当前完整任务分解

用户提出的三项任务是主线，但要形成可验证系统，还必须补齐观测、多堆环境、安全约束和公平评价。

### 任务 A：动作耦合的在线健康状态更新

目标：让各堆健康状态随实际决策变化，而不是始终使用固定 `theta_late`。

第一版采用可解释的 Gamma 累计退化状态：

```text
D_i(k+1) = D_i(k) + DeltaD_i(k)
DeltaD_i(k) ~ Gamma(alpha_i(action,event,dt), beta_i)
theta_i(k) = g_i(D_i(k))
```

其中均值退化强度至少区分：

- 负载幅值/电流档位；
- 负载变化；
- 启停；
- 怠速/低功率；
- 停机休息；
- 堆间随机异质性。

第一版不声称从现有数据精确辨识了所有因果系数。均值结构参考 Pei/Ghaderi/Zuo，Gamma 过程负责不可逆正增量和随机性；刘占伟 `theta(k)` 用于健康轨迹尺度映射和合理性验证。

还必须增加观测修正接口：

```text
prediction:  D_i^- / theta_i^-
observation: current, voltage, temperature, pressure ...
correction:  UKF/PF/FFRLS or a simplified calibrated observer
posterior:   D_i^+ / theta_i^+
```

没有新观测时只能做状态预测，不能宣称完成了在线观测修正。

### 任务 B：真正的多堆功率分配环境

目标：由单堆 `P_fc + P_bat` 扩展为：

```text
P_dem = sum(P_fc_i) + P_bat
u_k = [I_1, I_2, ..., I_n, on/off_1, ..., on/off_n]
```

环境状态至少包括：

- 当前需求功率与短时预测；
- SOC、电池功率与上一动作；
- 每堆 `D_i/theta_i/SOH_i`；
- 每堆电流、开关状态、驻留时间和启停计数；
- 怠速、高载、变载、制动、SOC紧张、健康失衡等事件。

约束至少包括：

- 瞬时功率平衡；
- 电池功率与SOC上下限；
- 电堆功率/电流范围；
- 最小开停机/档位驻留时间；
- Ramp与启停限制；
- 实验末端SOC等值。

必须先实现枚举/Beam-search 多堆基线，再替换为高级算法，否则无法判断高级算法的真实收益。

### 任务 C：未来需求功率预测优化

预测对象固定为整车需求功率 `P_dem`，因为燃料电池需求功率是控制决策结果。

后续按以下层次比较：

1. Persistence/Constant、RLS/AR和现有XGBoost基线；
2. 速度预测→车辆动力学换算的物理基线；
3. 工况/事件条件化的多专家预测器，重点处理制动、高功率和突变事件；
4. 机理预测 + 学习残差；
5. TCN/GRU/PatchTST等直接多步序列模型；
6. 分位数或场景式概率预测，将不确定性传入鲁棒/场景MPC。

预测模型不能只按总体RMSE选择。必须同时评价：

- MAE、RMSE及规范化指标；
- 未来窗口能量误差；
- 高功率和制动事件F1；
- 概率预测覆盖率/校准度；
- 相对Perfect-preview的控制决策后悔值；
- 对最终氢耗、退化、SOC和切换的下游收益。

当前最值得优先验证的是“事件条件化 + 概率多步预测”，因为现有主要误差来自缺少外部触发信息的突发制动/负载变化；仅继续增加普通历史统计特征，已有消融结果显示收益很小。

### 任务 D：模块化机理世界模型

在引入 Dreamer/TD-MPC/EAWM 前，先建立可调用、可测试的一步转移接口：

```text
next_state, cost, constraint_info = world_model.step(state, action, demand)
```

内部由以下模块组成：

- 电堆 `I,theta → V,P,H2,efficiency`；
- Gamma健康转移 `D,action,event → D_next/theta_next`；
- 电池 `SOC,P_bat → SOC_next`；
- 事件更新；
- 氢耗、FC退化、电池使用、SOC、波动和启停成本；
- 动作掩码和硬约束检查。

未来SOC、退化、健康状态和氢耗由该模型递推得到，而不是作为外部已知输入。

### 任务 E：状态观测、校准与可辨识性

需要明确区分：

- 状态：`D/theta/SOH`；
- 动作：各堆电流、功率和开关；
- 观测：电压、电流、温度、压力等传感量；
- 代理成本：`C_deg(action | state)`；
- 真实材料退化：当前数据无法直接完整观测。

必须完成单位、时间步长和尺度审计，避免把“每事件”“每秒”“每小时”的参数混用。任何暂用文献或仿真参数都写入配置并标注来源，不写死在算法内部。

### 任务 F：安全投影与公平实验协议

高级策略输出不能直接执行，必须经过：

```text
动作掩码 → 可行性检查 → 安全投影/最近可行动作 → 执行
```

所有策略使用相同的数据切分、模型、动作空间、SOC条件和可行域。最终主比较应满足硬末端SOC约束，例如 `|SOC_end-SOC_ref|<=0.001`；若临时不能满足，必须使用SOC等值补能指标并同时报告原始值。

实验至少包含：

- Average / Daisy-chain / Rotating / Instant；
- Constant/Predicted/Perfect-preview MPC；
- 退化不感知与退化感知；
- 确定性退化与Gamma随机退化；
- 单堆/双堆/三堆及健康异质性；
- 多工况、多随机种子、消融和敏感性分析。

### 任务 G：世界模型与高级算法升级

算法按下列顺序升级，不直接跳到最终模型：

#### Phase G1：机理/表格世界模型 + Beam-search MPC

用于证明状态、动作、健康转移、约束和评价闭环正确，是所有学习算法的对照组。

#### Phase G2：机理模型 + 学习残差

用 MLP/GRU/XGBoost 学习一步误差：

```text
real_next_state = mechanistic_next_state + learned_residual
```

先验证学习模块是否改善多步 rollout，而不是只看一步训练误差。

#### Phase G3：Dreamer-style RSSM + EAWM事件增强

Dreamer作为潜在状态世界模型和 imagined rollout 主体；EAWM增加事件预测、事件边界检测和事件感知表征，使模型关注启停、制动、高载、变载、SOC紧张和健康失衡等关键事件。

EAWM不是独立控制器，而是可以接入Dreamer等世界模型的表征增强模块。

#### Phase G4：规划与安全执行

保留MPC/Beam-search或安全投影层，确保学习策略不违反硬约束。可比较：

- Dreamer actor直接给出候选动作 + 安全投影；
- Dreamer世界模型内 imagined rollout + MPC选择；
- EAWM-Dreamer-MPC完整模型。

TD-MPC2作为重要参考和对照，但暂不作为当前离散档位环境的首选主算法。其原论文主要面向连续动作，并明确指出离散动作规划扩展仍是开放问题。如果后续把各堆功率放松为连续动作，或采用适合离散组合动作的搜索器，才进入直接TD-MPC式实现。

## 5. 推荐实施顺序

| 阶段 | 目标 | 主要交付 | 状态 |
|---|---|---|---|
| P0 | 同步仓库、恢复关键数据、验证冻结baseline | 可复现环境与本追踪文档 | 已完成 |
| P1 | Gamma在线健康状态原型 | 状态类、转移模型、配置、单元测试、标定/随机轨迹 | 已完成（方差为敏感性假设） |
| P2 | 动态 `C_deg(I|theta)` 与观测修正接口 | 健康相关在线计算、observer接口 | 动态代理已完成；观测修正待MAT观测链 |
| P3 | 多堆机理世界模型与Beam-search MPC | 双/三堆闭环、基线策略、约束测试 | 已完成；统一随机测试框架和在线健康不变量已通过 |
| P4 | 需求功率预测升级 | 事件/概率预测基准与下游控制评价 | 候选原型已完成；暂停扩展，回到统一测试框架 |
| P5 | 残差学习世界模型 | 一步与多步rollout实验 | 待开始 |
| P6 | Dreamer/EAWM高级模型 | EADWM-MPC、消融和安全投影 | 待开始 |
| P7 | 多工况、统计和论文级验证 | 公平比较、敏感性、最终图表和结论边界 | 已建立10种子配对框架；仍需扩展样本和参数校准 |

原则：一次只推进一个可以验证的接口；上一阶段没有通过，不进入下一阶段堆算法。

## 6. 各阶段最低验收条件

### P1健康更新

- 固定随机种子可复现；
- Gamma增量非负，健康状态不会无解释地逆向恢复；
- 高负载、变载和启停能够改变均值退化强度；
- 不同堆可配置异质性；
- 单位和时间步长明确；
- 确定性均值模式可用于调试。

### P2动态代理与修正

- `C_deg(I|theta_early/middle/late)`随健康阶段发生合理变化；
- 预测状态和观测修正状态分开记录；
- 无观测时不伪造posterior更新；
- 与现有late静态表可回归对齐。

### P3多堆闭环

- 每一步功率平衡误差在数值容差内；
- SOC、电池功率、驻留和动作约束零违规；
- 能复现Average/Rotating等简单策略；
- 老化堆能够在健康感知策略下获得更多休息或更低负载；
- 末端SOC公平。

### P4预测升级

- 时间顺序切分且H秒purge；
- 不使用未来泄漏字段；
- 至少在事件指标、概率校准或控制收益之一稳定超过当前XGBoost；
- 如果只降低总体RMSE但不改善控制，不升级默认模型。

### P5/P6学习世界模型

- 多步rollout误差不发散；
- 在未见运行片段上评价；
- 学习策略经过安全投影后零硬约束违规；
- 相对机理MPC的提升在多工况和多随机种子上存在，而不是单次偶然结果；
- 必须做事件模块、Gamma模块、latent模型和规划器消融。

## 7. 当前缺失但后续需要的材料

下列内容在需要相应阶段时再向用户索取，不阻塞P0文档和基础代码审计：

1. `data_mark_update/a/b/bestX/V/v_test` 等MAT观测链，用于复现UKF-PF并实现真实观测修正；
2. 多堆实测数据中各堆电流、电压和开关状态的最终字段口径，用于P3外部数据验证；
3. 训练Dreamer/EAWM时可用的GPU环境、显存大小和允许训练时长；
4. 若本地还有未推送代码或报告，需要先同步，避免重复实现。

已收到并不再阻塞：6104事件表、完整逐事件theta轨迹、theta物理/元数据、当前点损失条件与表、Python IV模型、MATLAB UKF-PF脚本，以及Pei/Ghaderi系数口径。

## 8. 自动实施与沟通规则

执行循环：

```text
读取当前代码/数据
→ 实现一个最小接口
→ 单元测试和小样本运行
→ 完整数据或多工况验证
→ 分析失败原因并迭代
→ 更新本文档
→ 提交可复现结果
```

可以自主完成：仓库内代码、测试、配置、实验脚本、结果分析和文档维护。

必须及时询问用户的情况：

- 需要仓库外原始数据或MATLAB工程；
- 参数存在多个互斥物理口径，选择会改变研究结论；
- 需要远程GPU/服务器权限；
- 需要对GitHub主分支、外部服务或他人资源做超出当前授权的变更；
- 现有数据无法支持计划中的科学声明。

## 9. 决策日志

### 2026-07-11：建立统一执行主线

- 决定先完成动作耦合Gamma健康转移和多堆机理闭环，再优化需求预测和引入世界模型算法。
- 决定最终算法以Dreamer-style RSSM为主体、EAWM为事件增强、MPC/安全投影为执行约束层。
- TD-MPC2保留为连续动作方向或对照，不直接套用到当前离散多堆档位。
- 当前degradation proxy继续作为性能损失决策量；新增Gamma状态承担动作到未来累计退化的转移作用，两者不混称。
- 预测升级最终以控制收益为判断标准，而不是只追求RMSE。

### 2026-07-11：P0复现与P1最小原型

- 最新仓库关键数据已通过 `scripts/00_materialize_key_data.py` 恢复。
- 完整baseline已从动力学、H=1/3/5/10 XGBoost预测运行到四策略功率分配和汇总检查。
- 动力学指标与冻结报告一致：MAE约8.55 kW、RMSE约17.85 kW、R2约0.877。
- 当前环境使用XGBoost 3.3.0，预测和Predicted MPC结果相对冻结结果存在很小的版本差异；因此不覆盖仓库冻结结果，后续应锁定依赖版本或保存模型文件。
- 新增 `src/fc_power/health/gamma_process.py`：显式区分Gamma负载幅值增量、自然退化、变载增量和启停增量，不内置未经审计的文献参数。
- 新增7项健康转移测试，覆盖非负单调性、负载敏感性、随机种子复现、启停、堆间异质性、SOH阈值和输入校验，均已通过。
- 当前运行环境无PyTorch和GPU；不阻塞P1-P4，P5-P6正式训练前需要补充学习环境。

### 2026-07-11：LZW上游接入、Gamma标定与动态代理

- 上游基线更新为 `7e71f39`；6104行事件表和theta轨迹按 `canonical_row_6104` 完全对齐，累计暴露末端约3264.15 h。
- 使用Ghaderi 2023 Table 4（引用Pei 2008）的固定系数形成累计动作损伤指数：末端9.353029%，其中启停2.536240%、高载0.034688%、低载0.073682%、自然运行6.411326%、变载0.297093%。这些数值定义相对暴露结构，不是对LZW堆材料退化率的独立因果辨识。
- 将损伤指数单调映射到LZW theta：`i0/ih/R_ohm`幂指数分别为2.0493/0.7408/1.6039，对应R2为0.9825/0.9407/0.9782。
- 单条长期轨迹不足以可靠辨识Gamma方差；设置终点CV=5%/10%/20%敏感性，默认10%对应Gamma scale=0.134177损伤百分比单位。
- 新增动态 `C_deg(I|theta_current)`：候选动作先更新Gamma损伤与theta，再由LZW IV模型计算电压、功率及归一化性能损失。late状态与上游370 A归一化表回归一致。
- 健康与动态代理共18项单元测试通过；3 MB逐事件Monte Carlo轨迹可由脚本重建，不纳入Git补丁，小型标定JSON、报告和验证表保留。
- P1完成；P2的动态代理完成。真实posterior观测修正明确延后到MAT观测链可用时，当前不伪造观测更新。

### 2026-07-11：P3多堆机理世界模型核心闭环

- 新增统一 `step(state, action, demand)`：状态包含SOC、每堆Gamma健康、电流、开关和驻留时间；一步输出theta、电压、堆功率、氢耗、电池补偿、下一SOC、成本分解与约束信息。
- 离散动作显式区分0 A停机与0 A带电怠速；不同堆可设置初始健康和Gamma异质性。
- 新增双/三堆动作枚举、Instant、Beam-search和最近可行动作安全投影，所有规划器复用同一机理转移，不另建隐式代价模型。
- 当前共29项单元测试通过；覆盖功率平衡、健康响应、异质堆输出、SOC、驻留、电池限制、动作网格、Beam rollout和安全投影。
- 32步双堆合成需求闭环零约束违规，最大功率平衡误差为0；三堆相同195 A下，健康/半老化/late堆功率为24.604847/24.470271/24.184576 kW。
- 发现4 s预测窗口短于15 s驻留时，会在低需求点关闭健康堆并迫使老化堆承担下一负载峰。将窗口覆盖完整驻留周期后，健康堆/老化堆累计电流由错误方向修正为4450/1350 A·step。该现象作为预测时域与约束耦合的必要消融保留。
- 上述合成序列只验证接口，不作为节氢或延寿结论；P3仍需在冻结真实测试段比较Average、Rotating、Instant和Beam，并执行末端SOC公平处理。

### 2026-07-11：P3冻结真实需求段公平基准

- 在冻结连续测试段前300 s上比较Average、Rotating、健康感知Instant和16 s perfect-preview Beam；初始损伤为10%/65% LZW late参考，双堆异质系数为1.0/1.1。
- 电池功率、SOC和功率平衡作为不可放宽硬约束；驻留规则只在急制动导致全动作不可行时允许带惩罚的安全覆盖，并在轨迹中单独记录。四策略硬约束违规均为0，最大平衡误差约7.1e-15 kW。
- Average与Beam通过 `|SOC_end-SOC_ref|<=0.001`。相对Average，Beam的SOC等值氢耗降低2.38%、Gamma增量降低4.81%、性能损失降低53.25%、电池吞吐降低14.28%。
- Beam对健康堆/老化堆累计分配15000/8100 A·step，符合老化堆减载方向；Average为12660/12660 A·step。
- Rotating和Instant末端SOC偏差约+0.0056，未进入严格主比较，只保留SOC等值筛查指标。
- Beam终端SOC权重300时通过公平门槛，但300 s轨迹运行约213 s；这证明当前枚举Beam可作为正确性基线，不能作为最终实时控制器，P5/P6需显著压缩规划耗时。

### 2026-07-12：P4事件条件化预测候选（暂不替换默认）

- 新增未来制动/高功率分类、事件残差中心与分组conformal区间；验证集按时间再拆为残差拟合和区间标定两部分，测试集不参与离线拟合。
- H=5/10测试MAE相对同次XGBoost分别改善0.92%/1.80%，高功率F1提高0.0114/0.0163；H=10制动F1下降0.0070，说明中心预测不能替代独立事件概率。
- 固定90%区间测试覆盖率为88.32%/87.69%；H步延迟的因果在线更新提高到89.04%/88.80%，仍欠校准，因此不作为默认鲁棒MPC区间。
- 在offset=900的120 s事件丰富窗口中，事件中心将preview MAE从22.04降至20.67 kW，并使末端SOC误差从-0.001064变为+0.000248；p05制动场景过于保守，不采用。
- 该候选代码和结果保留，但按用户要求暂停继续调预测，先建立随机动态负载、多随机种子、多堆异质性和统一策略评价框架。

### 2026-07-12：统一多堆随机测试框架与在线健康闭环验收

- 新增事件标注的半马尔可夫合成压力负载和实测连续块重采样。合成转移概率、事件持续时间、功率范围、噪声和爬坡均显式配置；实测重采样只从全有限连续块取样，并按块边界跳变大小筛选拼接。
- 新增统一双/三堆测试执行器。每一决策步依次执行“当前健康→分配动作→Gamma条件均值/随机增量→新损伤→theta→IV/功率→下一SOC”，并强制检查损伤非负、`D_after=D_before+increment`、下一步`D_before=上一步D_after`。
- 所有策略使用相同负载、初始健康和SOC；主负载后追加共同SOC恢复尾段，尾段氢耗和退化不隐藏，分别记录主段/恢复段与总指标。
- 负载上界保留初始燃料电池容量的1%作为显式余量。该设计来自种子2035的边界失败：需求被裁到初始上界206.624662 kW，39步在线老化后上界降至206.624123 kW，0.00054 kW差值会使严格硬约束无解。新增45步上界压力回归测试后零违规。
- 支持Average、Rotating、Instant-health、Beam-health；三堆烟雾实验通过。三堆完整动作网格从双堆81组合增至729组合，确认枚举规划只适合作为正确性基线。
- 10个种子、60 s主负载+90 s共同SOC恢复、双堆Average/Beam实验全部达到`|SOC_end-SOC_ref|<=0.001`且零硬约束违规。
- 同种子配对结果：在实测块上Beam相对Average的主段期望退化均值为-0.000334个百分点（-7.49%，95% CI ±0.000424，8/10改善）；在合成负载上为-0.000050个百分点（-1.08%，95% CI ±0.000061，8/10改善）。两者区间仍跨0，不能宣称Beam稳定延寿。
- 合成负载上SOC等值氢耗差为-0.718 g（-0.99%，95% CI ±0.490，8/10改善），总电池吞吐差为-0.147 kWh（-28.03%，95% CI ±0.056，10/10改善）；其余多数指标方向有利但区间跨0。
- 短时随机Gamma轨迹因每秒shape极小而呈稀疏跳变，CV=5%/10%/20%在数分钟轨迹上几乎不改变比较，不能作为短时控制创新点。改用1000 h等效暴露聚合后，CV只扩大分布宽度，不改变场景排序。
- 长期敏感性中确定性启停/变载事件占总期望损伤96.9%–98.2%，当前策略排序主要受事件定义与文献系数控制；一个合成种子中Beam反而更差。后续必须优先审计事件系数和频次尺度，Gamma保留为不可逆在线状态与边界不确定性。
- 测试报告增加Average参照的同种子配对差值、相对变化、95% CI和改善率，避免把负载随机性误报为控制器收益。

### 2026-07-12：高级世界模型材料适配结论

- EAWM的核心是为基础世界模型增加自动事件生成、事件预测和Generic Event Segmentor，并通过辅助目标塑造潜在表示；它不是独立控制器。当前PEMFC事件已有物理定义，因此第一版应使用高载、制动、启停、变载、SOC紧张和健康失衡作为多模态事件标签，而不是照搬视觉AGMM。
- TD-MPC2原始问题定义和规划器面向连续动作，使用潜在动力学、奖励/Q预测、策略先验和采样式局部轨迹优化。当前离散多堆档位不能直接照搬其连续采样规划；可借用decoder-free latent dynamics与TD价值头，但动作选择仍需离散Beam或安全投影。
- 因此高级算法顺序保持不变：先做机理残差的一步/多步rollout基准，再做Dreamer-style潜在动力学，最后添加EAWM事件辅助头；任何学习策略都必须通过当前统一测试框架和安全执行层。

## 10. 下一步唯一优先任务

优先完成退化事件系数与时间尺度敏感性门槛：把启停、变载、自然/负载连续损伤系数配置化，在统一10种子配对框架中做一因素和联合敏感性，确认策略排序对系数不确定性的稳定区间。原因是当前长期损伤的96.9%–98.2%由确定性事件项主导；在该口径未经审计前训练Dreamer/EAWM，只会更快地优化一个可能失真的目标。完成后进入P5机理残差世界模型的一步/多步rollout基准。
