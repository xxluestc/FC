# 多堆燃料电池退化感知功率调控总计划

更新时间：2026-07-13
持续维护分支：`codex/gamma-health-foundation`

本文档是项目当前范围、优先级、阶段门槛和决策状态的唯一持续维护入口。
`CODEX_CLI_HANDOFF_2026-07-12.md`保留为一次性交接快照，
`project_execution_tracker.md`保留详细历史和实验日志；二者不再决定当前优先级。
快速状态和图表计划见`docs/PROJECT_STATUS_BOARD.md`。

## 1. 当前研究问题

当前首先解决三堆PEMFC系统内部的运行堆选择和功率分配：

```text
当前已知需求功率
+ 三个电堆各自的D/theta/IV状态、电流、开关和驻留状态
        ↓
候选动作[I1, I2, I3, on1, on2, on3]
        ↓
三堆N+1硬约束：最多两个堆在线，一个堆轮换休息
        ↓
实际执行动作驱动各堆确定性健康更新
D_i(k+1) → theta_i(k+1) → V_i/P_i(k+1)
        ↓
满足燃料电池侧目标功率，同时权衡氢耗、退化、性能损失、启停和变载
```

近期不把锂电池、SOC恢复和未来需求预测放入主决策问题。它们是后续外层优化，
不能阻塞燃料电池内层基础闭环。

## 2. 冻结边界

除非形成新的书面决策，以下口径保持不变：

| 项目 | 当前口径 |
|---|---|
| 系统结构 | 三堆适度超容量N+1，同一时刻最多两个堆在线 |
| 当前决策层 | 多个燃料电池之间的运行堆选择和功率分配 |
| 当前负载信息 | 只使用当前需求，不做未来需求预测 |
| 锂电池 | 暂不进入当前内层决策；保留未来外层接口 |
| 健康更新顺序 | 确定性动作更新 → Gamma随机退化 → 实车观测校正 |
| 单堆负载字段 | `liu_vehicle_canonical_1s.csv:fc_input_power_kw` |
| 标定/留出边界 | segment 0-21标定；segment 22-45完整留出 |
| 退化代理口径 | `C_deg(I|theta)`是当前健康下性能损失代理，不是材料退化直接观测 |
| Gamma口径 | 秒级控制用条件均值；完整分布只进入小时级风险/维护分析 |
| 文献系数 | Ghaderi/Pei系数必须标来源，不称为LZW数据因果辨识 |
| 高级算法 | 基础门槛通过前不训练Dreamer/EAWM/TD-MPC类模型 |

## 3. 证据优先级

研究判断按以下顺序取证：

1. 本地原始数据、作者MAT结果链和可复现代码输出。
2. `G:\大论文\AI文献库`中的论文原文、公式、表格和实验设置。
3. 仓库内审计报告、单元测试和冻结实验结果。
4. 明确标注的工程假设、敏感性范围和模型推断。
5. DeepSeek、GPT网页对话等辅助意见，仅作为待核实线索。

辅助模型给出的数字、公式、页码、单位和结论必须回到论文或代码复核，才能进入模型和报告。

## 4. 阶段门槛

### G0：仓库与数据可复现

状态：已通过。

- 交接补丁完整应用；
- 关键数据可物化；
- 87项测试、baseline、compileall、`git diff --check`通过；
- 受保护数据文件未被修改。

### G1：三堆确定性健康基础闭环

状态：已通过。

已验收：

- 动作枚举和世界模型均强制最多两个堆在线；
- 每堆只根据自身实际执行动作更新D；
- D单调更新后映射theta，再由统一IV模型更新电压和可用功率；
- 关机休息堆连续退化为零，0 A带电怠速与完全停机保持区分；
- 规划动作与实际执行动作、确定性增量与随机增量分别记录；
- 增加三堆轮休、长期停机零退化、变载计数一致性回归测试。

出口证据：三堆运行15步后执行轮换，休息堆保持零退化，轮换后各堆按实际动作更新；
`D→theta`逐堆一致，世界模型与暴露提取器的变载计数一致，N+1硬约束通过。

### G2：燃料电池内层功率接口

状态：已通过基础出口验收。

世界模型现在显式支持历史`battery`和新增`fc_only`两种功率接口。`fc_only`必须显式给出
跟踪容差，电池功率固定为0、SOC冻结，跟踪误差进入独立成本和硬约束；旧电池模式保持默认。

已完成：

- 定义不依赖电池/SOC的燃料电池侧目标功率和跟踪误差；
- 保留离散电流、驻留、爬坡、启停和最多两堆在线约束；
- 明确离散档位下允许的功率跟踪容差或局部连续修正方式；
- 旧混合系统模式保持兼容，供未来外层氢电分配复用；
- FC-only评价不再使用SOC修正氢耗、电池吞吐或SOC恢复尾段。

当前8档电流、两堆在线的健康聚合功率最大相邻间隔约10.60 kW，半间隔约5.30 kW；
5.5 kW仅作为由当前动作网格推导的候选容差，不是通用物理常数。Average、Rotating、Instant、
Beam、安全投影和统一测试床均已通过FC-only单元/短轨迹测试，SOC恢复直接调用会被拒绝。

实车1秒基线与Zuo快/慢独立压力场景的三条120步确定性轨迹均零违规，最大跟踪误差
4.018 kW；四状态、四策略静态动作检查最大误差同为4.018 kW，均低于5.5 kW候选容差。
该容差仍需在后续长轨迹和健康演化中复核，不能通过任意放宽掩盖不可行区间。

### G3：Zuo-实车随机动态负载

状态：已通过基础出口验收。

已完成：

- 核对Zuo 2024三堆两运行、无电池、四状态Markov结构；
- 实车单堆数据按最大时间间隔切成标定段与完整留出段；
- Zuo相对状态在标定段的占比审计；
- 1/5/10/30/60秒转移矩阵和segment级bootstrap 95%区间。
- 新增分段常值四状态生成器，显式输入转移矩阵、决策间隔、初始分布、容量参考和种子；
- 审计1/5/10/30/60秒经验矩阵的等效状态变化率，确认下采样会漏掉中间变化；
- 取消不同时间基准的经验矩阵与Zuo矩阵直接凸组合；
- 选择实车1秒经验矩阵作为主标定场景，保持原生1秒时间基准；
- 将Zuo慢变/快变矩阵保留为独立压力场景，30秒转移步明确标为工程候选；
- 负载上限映射为两台健康堆容量的95%，正需求时强制恰好两堆在线；
- 0/2/5/10/15%容量余量在健康、异质和后期三类状态下完成动作覆盖扫描；
- 三条120步FC-only确定性轨迹零违规，未使用留出段功率分布。

后续验证：

- 在确定性多策略比较中复核5%容量余量和5.5 kW容差的长轨迹覆盖；
- 随机负载用于开发，segment 22-45仅用于最终连续真实回放。

出口条件：状态、转移、时间尺度、容量映射和随机种子全部可追溯。

### G4：确定性控制基线和公平评价

状态：基础出口已通过；保留全量86,415秒回放为后续计算扩展。

比较Average、Rotating、Instant-health和Beam-health。核心指标为燃料电池功率跟踪误差、
总氢耗、各堆退化与离散度、性能损失、启停/变载、在线/轮休时长、连续在线/停机时长、
计算时间和候选动作数。FC-only主实验不使用SOC或电池指标。

已完成：

- 修正Beam的信息边界：只使用当前需求保持预览，不读取真实未来需求；
- 记录逐步规划时间、展开/可行候选数、逐堆启停/变载和最长连续在线/停机时长；
- 补充FC输出电量与单位输出电量氢耗，避免把欠跟踪造成的低总氢耗误判为高效率；
- 新增场景-策略进程并行，短轨迹串/并行物理指标逐字段一致；
- 三个独立负载场景、10种子、120秒比较共120次运行均零违规且100%位于5.5 kW容差内；
- 当前保持预览下Beam与Instant动作和物理结果相同，但规划耗时约为Instant的两倍；
- Rotating改善轮休均衡，但额外启停使相对Average的总期望退化约增加85%-101%；
- Instant/Beam相对Average的跟踪MAE降低约54%-62%，氢耗强度上升约1.7%-3.7%；
- Instant/Beam期望退化均值降低约0.7%-1.6%，但配对区间并非三个场景都稳定低于0，只能解释为小幅倾向；
- Average跟踪MAE约2.75 kW，不能与约1.05-1.34 kW的Instant/Beam脱离输出电量直接排序。

真实留出验证：

- segment 22-45每段按时间中心固定选取最多120秒，短段全部保留；选择规则不查看功率值；
- 24个窗口覆盖6个正功率运行段和18个完整停机段，参数全部冻结；
- Average因严格等电流限制在segment 32、42无可行动作，成功22/24；Rotating、Instant、Beam均24/24；
- 所有成功运行均零硬违规且100%位于跟踪容差内；失败不通过放宽容差或替换策略掩盖；
- 6个正功率窗口中Beam相对Instant的跟踪MAE差值为-0.016 ± 0.032 kW，区间跨0，规划时间增加约107%；
- 当前将Instant作为确定性健康基线，Beam保留为正确性和后续消融上界；不声称已完成86,415秒全量回放。

### G5：Gamma随机退化与统计评价

状态：基础诊断已完成；Gamma保留为聚合暴露不确定性，不作为120秒控制排序的采样指标。

- 完成三类负载、10个联合配对种子、三策略共90次在线随机健康运行，所有运行零硬违规；
- 负载种子与健康种子一一映射并在策略间共享，但不是独立双因素全排列；
- 120秒连续Gamma shape总量仅约`8.3e-4`-`1.5e-3`，90/90次运行的采样连续增量低于`1e-15%`；
- 解析时间尺度审计显示，120秒采样低于条件均值1%的概率约98.7%-99.0%，不适合估计均值；
- 冻结动作暴露聚合到1000小时后shape约26-37，可用于Gamma时间尺度诊断；
- 聚合实验中离散事件项占总期望退化约95%-99%，CV扫描没有改变策略方向，排序主要由事件系数主导；
- 旧1000小时实验同时重复了120秒内的启停事件，隐含不合理高频启停，已降级为历史诊断，不能支撑寿命或方法有效性；
- 控制决策继续使用确定性条件期望；Gamma用于长时间尺度敏感性，不声称寿命预测。

### G6：实车观测校正

状态：等待MAT观测链。先定义`HealthObserver.predict/correct`接口和合成观测测试；
获得完整变量后再复现UKF/PF posterior。在此之前只能称为动作驱动预测态。

### G7：基础模型消融与双时间尺度优化

状态：开发方法已完成强基线审查，24小时低频health-greedy已通过冻结真实留出验证。

- 默认系数下Instant相对Average总退化仅降低约0.66%-1.16%，每场景4/5种子获胜；
- 跨75点网格，Instant相对Average逐样本获胜率仅60%-80%，短时优势不稳健；
- Instant相对Rotating的大幅优势主要来自避免固定轮换启停，不能单独作为论文贡献；
- 快层现可被慢层固定到指定两堆，仍逐秒执行动作枚举、D→theta→IV/功率和跟踪约束；
- 开发模板扩为120条300秒快层闭环轨迹：48个segment 0-21连续实车窗，另有三类各24个压力模板；
- 完整校准段辨识24次启动，即0.690次/观测小时，segment-bootstrap 95%区间0.300-1.653；
- 启停修正的实车主模板上，固定双堆、health-greedy、Expected-max和Gamma-CVaR的20种子
  平均边界时间分别为1287.45、1680.80、1682.40和1646.10小时；
- Expected-max相对health-greedy仅平均增加1.6小时，范围-5至24小时，获胜率30%；
  在经验Markov、Zuo-slow和Zuo-fast上亦只增加1.5/2.6/3.7小时，不能作为独立贡献；
- 主方法因此冻结为24小时低频health-greedy；Expected-max和Gamma-CVaR保留为消融；
- 健康边界、连续、变载、真实运行启停和调度启停共11组单因素扫描中，health-greedy
  相对固定双堆全部10/10获胜，最小配对增益至少259小时；
- 滞回阈值0.005虽将平均启动36.2降至19.8且平均时间增加2.3小时，但最差种子损失21小时；
  无阈值通过冻结的逐种子非劣规则，因此不进入主方法；
- 健康边界仍是LZW轨迹终点而非失效阈值，长期数字只称边界到达时间；
- 下一出口：冻结所有开发参数，只在segment 22-45验证快层可行性与慢层选择质量。
- 冻结留出选择验证已完成：6个正功率中心窗口乘3个预声明健康身份共18个案例，
  逐案事后枚举6个有序分配；health-greedy 18/18可行、oracle在线集合命中100%、
  最大退化遗憾为0，最大跟踪误差5.172 kW；
- Expected-max把有序角色命中率从66.7%提高到100%，但终端最大退化遗憾同为0，
  再次证明复杂角色目标不应进入主方法；
- 完整留出回放已覆盖segment 22-45全部86,415个1秒样本、3种健康身份和2种冻结策略，
  共144例、518,490个闭环步；全部零约束违规，最大跟踪误差5.499 kW；
- 每个完整segment内连续携带动作、驻留和健康状态，段间不跨越未知时间缺口；8个正功率段中，
  固定集合包含最老堆时health-greedy 8/8降低终端最大退化，均值降低0.009619/0.009931个百分点；
- 以8个完整运行segment而非逐秒步作为统计单位，两个均值差的95% bootstrap区间分别为
  [-0.014611,-0.005263]和[-0.015213,-0.005395]个百分点，单侧精确Wilcoxon经Holm校正后
  均为`p=0.0078125`；
- 留出segment最长7.543小时，小于24小时慢层周期，因此全段回放验证入口堆选择与段内快层，
  不声称验证了留出数据上的动态24小时重复重调度；
- 该均衡收益伴随总期望退化增加0.005508/0.006375个百分点和跟踪MAE约增加0.035 kW，
  说明方法优化的是最差堆健康而非所有指标，不宣称Pareto支配；
- 冻结30 kW参考不是物理额定值。完整留出中7,232个正功率样本被截到归一化1.0；
  最坏健康/策略下不截峰时6.65%正功率步超过双堆物理容量，事后严格容量参考下界为36.756 kW；
- 留出期40 kW控制目标可作为无截峰候选，但在获得21UBE0022额定资料前不得回填开发标定。
- 事后35/40 kW全量回放将正功率截峰降至2.672%/0%，两组均144/144完成、零硬违规；
  最老堆0/1在30/35/40 kW六个主比较中均8/8改善且95% segment-bootstrap区间低于0，说明
  最差堆改善方向不依赖30 kW截峰，但不改变物理额定值待确认的边界；
- 逐段删一后两项主检验全部保留显著，最大Holm校正`p=0.015625`；16个单段符号反转压力情景
  有6个失去显著性，说明现有观测不由单段驱动但外推仍受`n=8`限制；
- 冻结最大误差案例的定向容差扫描中4.90 kW及以下失败、4.95 kW成功，5.5 kW相对当前最小
  测试成功值有0.55 kW余量；该结果解释工程容差但不声称全留出更紧容差均可行；
- 四种冻结开发暴露与三种健康身份的12个慢层决策点中，health-greedy、Expected-max和
  Gamma-CVaR在线集合12/12一致，两类目标最大遗憾均为0；复杂目标自身最优只作实现一致性，
  不作为独立性能贡献；

### G8：后续外层和高级算法

状态：冻结。按顺序考虑锂电池外层、未来需求预测、机理残差世界模型、Dreamer/EAWM，
连续动作条件成熟时再评估TD-MPC类方法。

## 5. 当前风险

| 风险 | 影响 | 当前处理 |
|---|---|---|
| FC-only容差只经过短候选负载 | 长时健康演化后可能无可行动作 | 已用518,490步留出回放验证零违规、最大误差5.499 kW |
| Zuo时间单位未由论文明确 | 压力场景驻留概率和变载次数依赖工程假设 | 与实车1秒主基线分开报告，保留时间尺度敏感性 |
| 退化事件系数主要来自文献 | 短时策略排序已确认会随系数翻转 | 慢层仅在边界组合闭环复核，报告稳健区间 |
| 单秒Gamma shape极小 | 短轨迹采样几乎总为零，样本均值严重不稳定 | 控制用条件期望，Gamma转到聚合暴露层 |
| 短窗口漏掉稀疏运行启停 | 低估实车长期事件损伤 | 用完整校准segment的启动率及bootstrap区间独立标定 |
| 三堆离散动作组合较多 | 枚举Beam难以实时 | 当前仅作正确性基线 |
| 真实观测校正缺MAT链 | 不能声称posterior健康估计 | 保持预测态边界 |
| 留出段被提前用于调参 | 真实回放失去独立性 | 不用留出段功率分布调参 |
| 30 kW归一化不是物理额定值 | 截峰掩盖40 kW新负载档与容量不足 | 单独报告11.59%截峰和最坏6.65%容量超限；等待额定资料 |

## 6. 代理协作规范

### DeepSeek

- 通过本地Claude Code前端调用，实际后端为DeepSeek；不设置额度上限。
- 可以多个只读代理并行，并按任务需要使用`--effort max`。
- 适合代码定位、日志整理、文献初步摘录、测试缺口清单和文档结构检查。
- 不直接决定物理口径、统计方法、研究优先级或最终实现。
- 默认不给写文件和Git权限；代理意外产生的临时文件应审查后删除。
- 数字、页码、单位、路径和逻辑必须由主执行者复核。

### GPT网页对话

当前执行环境无法访问用户在VS Code中打开的浏览器会话。需要额外判断时，由主执行者生成
可直接粘贴的问题包，用户负责发送并回传结果。网页回答仍需本地证据复核。

## 7. Git与实验管理

- 每个独立阶段形成单独提交，不混合框架修改、数据审计和实验结果。
- 提交前运行相关测试、`compileall`和`git diff --check`；阶段出口运行完整测试和baseline。
- 结果必须带脚本、元数据、随机种子、输入来源和解释边界。
- 大型可再生轨迹不提交；小型审计表、元数据和报告可以提交。
- 不覆盖未知工作树修改；受保护数据文件不得暂存或还原。
- 每次阶段提交后更新本文档状态、风险和提交索引。

## 8. 提交与证据索引

| 提交 | 内容 | 关联门槛 |
|---|---|---|
| `7feb6dd`-`1b8e8de` | Gamma、LZW映射、世界模型、测试框架、退化尺度审计 | 历史基础 |
| `b2505f5` | 网页环境交接快照 | G0 |
| `1c14385` | 三堆N+1最多两堆在线硬约束 | G1 |
| `3842a16` | Zuo实车负载时间隔离与多尺度审计 | G3 |
| `54b4a87` | 三堆确定性健康轮换与变载计数回归 | G1 |
| `ba42b7c` | FC-only内层功率接口和统一测试床 | G2 |
| `b279395` | Zuo/实车候选负载映射与确定性验证 | G2-G3 |
| `2617872` | 时间一致的负载场景选择与容量余量敏感性 | G3 |
| `198092e` | FC-only无未来泄漏的确定性策略基础比较 | G4 |
| `6865dcb` | 确定性策略比较进程并行与3种子检查 | G4 |
| `6e98cad` | 120秒10种子确定性随机负载比较 | G4 |
| `2d26bab` | segment 22-45固定中心窗口真实回放 | G4 |
| `dc66004` | G3-G4论文图与可维护项目状态表 | G3-G4 |
| `399290c` | FC-only在线Gamma诊断、聚合暴露敏感性与论文图 | G5 |
| `12e7fb0` | 120模板、完整校准启停率、强基线、稳健性与主方法降级 | G7 |
| `ba66aac` | 冻结慢层在真实留出窗口的oracle选择验证 | G7 |
| `d2920cb` | 全部真实留出连续块回放、容量外推审计与Fig.14-15 | G3/G7 |

关键证据路径：`data/results/load_zuo_calibration/`、`data/results/fc_only_load_sensitivity/`、
`data/results/fc_only_zuo_foundation/`、`data/results/fc_only_deterministic_comparison/`、
`data/results/fc_only_real_holdout_replay/`、`data/results/testbed_multiseed/`、
`data/results/testbed_gamma_long/`、`data/results/testbed_coefficient_sensitivity/`和
`data/results/testbed_closed_loop_step_norm_smoke/`。

## 9. 需要用户协助

当前需要确认21UBE0022燃料电池额定净功率或控制器设计上限是否为40 kW；若无法提供，
主文保留30 kW冻结包络验证，并把40 kW仅作为事后容量候选。G6仍需要完整MAT观测变量链。

## 10. 当前唯一执行任务

冻结现有方法和留出结果，对论文核心章节执行主张一致性与审稿人视角自审；同时等待
21UBE0022物理额定功率确认。不得用留出峰值回填标定。G6继续等待MAT变量链；不恢复需求
预测，不加入锂电池外层，也不训练高级算法。
